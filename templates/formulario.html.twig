{% extends 'base.html.twig' %}

{% block title %}Formulario Artículo{% endblock %}

{% block body %}

    <h1 class="text-center display-5 mt-3">{{mensaje}}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="row mb-4">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" value="agregar" class="btn btn btn-outline-success">Agregar</button>
            <a class="btn btn btn-outline-danger" href="/" role="button">Volver</a>
            <a class="btn btn btn-outline-dark" href="/nuevo" role="button">Reset</a>
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}